name: Apply Unit Entry Finish State
on:
  push:
    branches: [fix/unit-entry-finish-controls]
    paths:
      - scripts/patch-unit-entry-finish-state.mjs
      - .github/workflows/apply-unit-entry-finish-state.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: fix/unit-entry-finish-controls
      - run: node scripts/patch-unit-entry-finish-state.mjs
      - run: node scripts/validate-app.mjs
      - run: node scripts/validate-production-readiness.mjs
      - name: Commit source changes
        run: |
          if git diff --quiet -- index.html; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Keep top and bottom unit finish controls in sync"
          git push origin HEAD:fix/unit-entry-finish-controls
