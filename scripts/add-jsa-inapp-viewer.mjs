import fs from 'node:fs';

const path = 'index.html';
let text = fs.readFileSync(path, 'utf8');

if (text.includes('id="jsaAttachmentViewer"')) {
  console.log('In-app JSA attachment viewer already present.');
  process.exit(0);
}

const styleAnchor = `.jsa-section h3{\n  margin-top:0;\n}\n`;
const styleBlock = `\n.jsa-attachment-viewer{\n  position:fixed;\n  inset:0;\n  z-index:100;\n  background:rgba(6,20,34,.78);\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  padding:18px;\n}\n\n.jsa-attachment-viewer.hidden{\n  display:none!important;\n}\n\n.jsa-attachment-viewer-card{\n  width:min(1000px,100%);\n  max-height:94vh;\n  overflow:auto;\n  background:white;\n  border-radius:18px;\n  padding:16px;\n  box-shadow:0 18px 60px rgba(0,0,0,.35);\n}\n\n.jsa-attachment-viewer-header{\n  display:flex;\n  align-items:flex-start;\n  justify-content:space-between;\n  gap:12px;\n  position:sticky;\n  top:-16px;\n  background:white;\n  padding:4px 0 12px;\n  z-index:2;\n}\n\n.jsa-attachment-viewer-header button{\n  width:auto;\n  margin:0;\n}\n\n.jsa-attachment-grid{\n  display:grid;\n  gap:16px;\n}\n\n.jsa-attachment-page{\n  border:1px solid var(--border);\n  border-radius:14px;\n  padding:10px;\n  background:#f8fafc;\n}\n\n.jsa-attachment-page img{\n  display:block;\n  width:100%;\n  height:auto;\n  max-height:78vh;\n  object-fit:contain;\n  border-radius:10px;\n  background:white;\n}\n\n.jsa-attachment-page iframe{\n  width:100%;\n  min-height:72vh;\n  border:0;\n  border-radius:10px;\n  background:white;\n}\n\n.jsa-attachment-page-title{\n  display:flex;\n  justify-content:space-between;\n  gap:10px;\n  align-items:center;\n  margin-bottom:8px;\n  font-weight:800;\n}\n\n@media(max-width:600px){\n  .jsa-attachment-viewer{padding:0;}\n  .jsa-attachment-viewer-card{border-radius:0;max-height:100vh;min-height:100vh;padding:12px;}\n  .jsa-attachment-viewer-header{top:-12px;}\n  .jsa-attachment-page iframe{min-height:68vh;}\n}\n`;
if (!text.includes(styleAnchor)) throw new Error('Style anchor not found');
text = text.replace(styleAnchor, styleAnchor + styleBlock);

const htmlAnchor = `  <div id="uploadedCompanyJsaSection" class="card hidden">`;
const viewerHtml = `  <div id="jsaAttachmentViewer" class="jsa-attachment-viewer hidden" role="dialog" aria-modal="true" aria-labelledby="jsaAttachmentViewerTitle">\n    <div class="jsa-attachment-viewer-card">\n      <div class="jsa-attachment-viewer-header">\n        <div>\n          <h2 id="jsaAttachmentViewerTitle" style="margin:0">JSA Attachments</h2>\n          <div id="jsaAttachmentViewerMeta" class="muted"></div>\n        </div>\n        <button id="closeJsaAttachmentViewer" type="button" class="secondary small">Close</button>\n      </div>\n      <div id="jsaAttachmentViewerContent" class="jsa-attachment-grid"></div>\n    </div>\n  </div>\n\n`;
if (!text.includes(htmlAnchor)) throw new Error('Uploaded JSA section anchor not found');
text = text.replace(htmlAnchor, viewerHtml + htmlAnchor);

const oldFn = `async function openUploadedJsaFiles(jsa){\n  const { data, error } = await sb.rpc('get_jsa_upload_attachments', { p_jsa_id:jsa.id });\n  if(error) return alert('Unable to load JSA files: ' + error.message);\n  if(!data?.length) return alert('No files are attached to this uploaded JSA.');\n  for(const attachment of data){\n    const signed = await sb.storage.from('jsa-uploads').createSignedUrl(attachment.storage_path, 300);\n    if(signed.error){ alert('Unable to open ' + attachment.original_filename + ': ' + signed.error.message); continue; }\n    window.open(signed.data.signedUrl, '_blank', 'noopener');\n  }\n}`;

const newFn = `function closeJsaAttachmentViewer(){\n  $('jsaAttachmentViewer').classList.add('hidden');\n  $('jsaAttachmentViewerContent').replaceChildren();\n}\n\n$('closeJsaAttachmentViewer').onclick = closeJsaAttachmentViewer;\n$('jsaAttachmentViewer').addEventListener('click', event => {\n  if(event.target === $('jsaAttachmentViewer')) closeJsaAttachmentViewer();\n});\ndocument.addEventListener('keydown', event => {\n  if(event.key === 'Escape' && !$('jsaAttachmentViewer').classList.contains('hidden')) closeJsaAttachmentViewer();\n});\n\nasync function openUploadedJsaFiles(jsa){\n  const { data, error } = await sb.rpc('get_jsa_upload_attachments', { p_jsa_id:jsa.id });\n  if(error) return alert('Unable to load JSA files: ' + error.message);\n  const attachments = [...(data || [])].sort((a,b) => (a.page_order || 0) - (b.page_order || 0));\n  if(!attachments.length) return alert('No files are attached to this uploaded JSA.');\n\n  const viewer = $('jsaAttachmentViewer');\n  const content = $('jsaAttachmentViewerContent');\n  $('jsaAttachmentViewerTitle').textContent = 'Company JSA';\n  $('jsaAttachmentViewerMeta').textContent = (jsa.work_date ? jsa.work_date + ' • ' : '') + attachments.length + ' attachment' + (attachments.length === 1 ? '' : 's');\n  content.replaceChildren();\n  viewer.classList.remove('hidden');\n\n  for(let i=0;i<attachments.length;i++){\n    const attachment = attachments[i];\n    const signed = await sb.storage.from('jsa-uploads').createSignedUrl(attachment.storage_path, 300);\n    const page = document.createElement('div');\n    page.className = 'jsa-attachment-page';\n\n    const title = document.createElement('div');\n    title.className = 'jsa-attachment-page-title';\n    const label = document.createElement('span');\n    label.textContent = 'Page ' + (i + 1) + ' of ' + attachments.length;\n    const name = document.createElement('span');\n    name.className = 'muted';\n    name.textContent = attachment.original_filename || '';\n    title.append(label, name);\n    page.appendChild(title);\n\n    if(signed.error){\n      const err = document.createElement('div');\n      err.className = 'muted';\n      err.textContent = 'Unable to load this attachment: ' + signed.error.message;\n      page.appendChild(err);\n      content.appendChild(page);\n      continue;\n    }\n\n    const url = signed.data.signedUrl;\n    const mime = String(attachment.mime_type || '').toLowerCase();\n    if(mime.startsWith('image/')){\n      const img = document.createElement('img');\n      img.src = url;\n      img.alt = 'JSA page ' + (i + 1);\n      img.loading = 'eager';\n      page.appendChild(img);\n    }else if(mime === 'application/pdf'){\n      const frame = document.createElement('iframe');\n      frame.src = url;\n      frame.title = 'JSA PDF ' + (i + 1);\n      page.appendChild(frame);\n    }else{\n      const link = document.createElement('a');\n      link.href = url;\n      link.target = '_blank';\n      link.rel = 'noopener';\n      link.textContent = 'Open attachment';\n      page.appendChild(link);\n    }\n    content.appendChild(page);\n  }\n}`;

if (!text.includes(oldFn)) throw new Error('Existing JSA open-files function not found');
text = text.replace(oldFn, newFn);

fs.writeFileSync(path, text);
console.log('Added in-app multi-attachment JSA viewer.');
