# JSA camera capture

The Upload Company JSA screen includes a field-friendly camera action for phone users. The camera input requests the rear-facing camera (`capture="environment"`) and allows the Foreman to take one page at a time. Repeated captures are queued with any existing PDF/image selections and saved through the existing private company-scoped JSA upload workflow.
